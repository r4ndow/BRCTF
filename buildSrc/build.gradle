plugins {
	id 'groovy-gradle-plugin'
}

repositories {
	gradlePluginPortal() // so that external plugins can be resolved in dependencies section
}

dependencies {
	// Provide sources for plugins. This is dumb but apparently the right way to do it...
	// https://docs.gradle.org/current/userguide/custom_plugins.html#applying_external_plugins_in_precompiled_script_plugins
	// <plugin id>:<plugin id>.gradle.plugin:<version>.
//	implementation "org.jetbrains.kotlin.jvm:org.jetbrains.kotlin.jvm.gradle.plugin:$kotlinVersion"
	implementation "com.github.johnrengelman.shadow:com.github.johnrengelman.shadow.gradle.plugin:$shadowVersion"
}